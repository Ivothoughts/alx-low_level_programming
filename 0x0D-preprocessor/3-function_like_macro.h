#ifndef FUNCTION_LIKE_MACRO_H

#define FUNCTION_LIKE_MACRO_H

/*
 * file: 3-function_like_macro.h
 * Description: Header file that defines function-like macro ABS(X)
 *   that compile the absolute value of a number X
 */

#define ABS(X) ((X) < 0 ? (-X) : (X))

#endif
